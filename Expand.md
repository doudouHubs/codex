# Expand：qmzhang 在 Codex 中新增的功能说明

> 本文面向产品使用者和项目评审者，按用户能感知的能力整理 Git 作者 `qmzhang` 在 Codex 分支中的新增功能。
>
> 统计范围：`qmzhang` 的个人提交，以及当前工作区尚未提交的功能改动。统计截至 2026-08-20。
>
> 状态说明：`已落地` 表示已经进入 Git 历史；`开发中` 表示当前仍在工作区，不能视为已发布能力。

## 功能总览

| 功能域 | 用户获得的能力 | 状态 |
| --- | --- | --- |
| 规则侧栏 | 在 TUI 中实时查看当前会话和项目生效的规则，并支持滚动、刷新和快捷键配置 | 已落地 |
| `# Prompt` 优化 | 在独立、受限的子线程中优化提示词，并把结果带回主线程 | 已落地 |
| TUI 鼠标交互 | 在文本框中按显示位置定位光标，兼容换行、中文、emoji 和不可编辑内容 | 已落地 |
| 输入体验 | 优化输入标记、光标基线、全角命令符号和粘贴内容处理 | 已落地 |
| 状态展示 | 统一 Prompt 与 Side 子线程的底栏状态和上下文提示 | 已落地 |
| 构建与发布 | 提供受控的本地 release 构建、清理和 Windows x64 npm 发布流程 | 已落地 |
| Plan 实施交接 | 在确认执行计划时区分上下文路径，并显示专属 `Plan Begins` 过渡效果 | 开发中 |
| `$expand` 文档维护 | 通过项目级 skill 按 Git 事实增量更新本文，并保留已提交/开发中状态边界 | 开发中 |

## 规则侧栏与快捷键体系

### 规则侧栏

通过默认快捷键 `Ctrl+T` 打开规则侧栏。侧栏会根据当前会话和项目根目录调用 `rule-system:rule-list`，把本次会话实际生效的规则展示在 TUI 右侧。

主要能力包括：

- 与主线程时间线和输入区并排显示，不需要离开当前会话。
- 支持按行滚动、按页滚动、顶部/底部跳转和鼠标滚轮滚动。
- 规则数量、加载中、刷新中、无规则和刷新失败等状态都有明确展示。
- 刷新失败时保留上一次成功加载的规则，并标记内容可能过期，避免短暂的插件故障清空用户正在查看的信息。
- 终端宽度不足时自动退出分栏布局，避免侧栏挤压主输入区。
- 全局快捷键、侧栏滚动快捷键和关闭快捷键都纳入统一 keymap，可在配置中覆盖，并参与快捷键冲突检查。

此外，项目中补充了规则系统的数据初始化，保证规则侧栏可以读取当前项目的规则集合。

对应提交：`4ab57978ad`、`48a7618fc0`。

## `# Prompt` 提示词优化工作流

### 使用入口

在主线程的空输入区输入 `#`，即可进入 Prompt 优化入口。优化参数只识别输入末尾连续出现的控制项：

- `--fast`：保持原提示词结构和长度，主要消除歧义、补齐表达。
- `--full`：在保留原始意图和约束的基础上，补足与任务直接相关的上下文和结构。
- 未指定参数时默认使用 `Full` 模式。
- 末尾控制项会从真正提交给优化器的正文中移除；正文中间出现的 `--fast` 不会被误删。

在 Prompt 模式中：

- `Enter` 提交当前文本进行优化或继续下一轮优化。
- Prompt 子线程中的 `Ctrl+Enter` 将最终优化结果交回主线程。
- `Esc` 或 `Ctrl+C` 取消当前流程，并恢复进入 Prompt 前的草稿和附件。
- Prompt 与 Side 子线程互斥，避免两个辅助输入面同时抢占同一套交互状态。

### 隔离的优化线程

Prompt 优化不直接复用主线程的执行上下文，而是创建独立的 Prompt 优化线程：

- 子线程是临时、只读的写作环境，只负责改写提示词，不执行命令、计划、审批、编辑或外部操作。
- 禁用 MCP、动态工具和写入能力，降低主线程工具能力被隐式带入的风险。
- 主线程只投影有界的最近用户请求和助手结果，不把项目规则、工具记录、内部推理和完整历史整体复制给优化器。
- 主线程刚启动、还没有持久化首个 turn 时也可以进入 Prompt，不再因为没有可 fork 的 rollout 而失败。
- 优化完成后可把结果交回主线程继续执行，取消或启动失败时会恢复原始草稿和图片附件。
- 底栏展示 `Prompt from main thread` 和当前优化模式，用户可以明确知道自己正在辅助线程中工作。

这套流程把“完善表达”和“执行任务”分开，既减少主线程上下文污染，也避免优化器误把历史中的待办事项当成需要立即执行的指令。

对应提交：`28af02290a`、`149e263f59`、`72090f0b85`、`e32c0f6c84`、`70ecaae769`、`8faef9d0f0`、`f128480824`、`446dcf37c1`。

## TUI 鼠标与输入体验

### 鼠标定位与覆盖层路由

- 统一 transcript、规则侧栏等全屏界面的输入路由，避免普通聊天快捷键穿透到当前覆盖层。
- 鼠标捕获只在界面请求且用户按住 `Ctrl` 时启用，释放最后一个 `Ctrl` 后立即归还终端，保留系统原生划选和滚轮行为。
- 点击文本框时，屏幕坐标会映射到最近的可编辑光标边界。
- 光标定位使用与渲染一致的换行和显示宽度计算，兼容中文、emoji、组合字符、显式换行、滚动文本和不可编辑的附件占位元素。

### 输入标记与全角符号

- 输入提示符与正文之间保留稳定的左右空白，提示符、正文和光标基线不会贴在一起。
- 当输入区为空且用户通过键盘输入首字符时，将常见全角命令符号转换为对应半角符号：`￥` 转为 `$`、`！` 转为 `!`、`、` 转为 `/`。
- 已有正文之后的全角字符保持原样，明确粘贴的全角内容也保持原样，避免破坏用户有意输入的文本。
- `!` 和 `/` 等命令入口可以直接通过中文输入法完成首字符触发，同时不影响普通粘贴内容。

对应提交：`0a2dd101b6`、`1b31122314`、`24685f0969`、`70ecaae769`。

## 状态展示与子线程体验

Prompt 和 Side 子线程统一使用底栏展示上下文标签、返回提示和当前状态，减少不同辅助线程之间的视觉差异。

这项改动同时确保：

- 进入和退出辅助线程时，主线程输入区的状态能够正确恢复。
- 子线程状态不会冒充主线程的普通工作状态。
- Prompt、Side、普通会话之间的输入可用性和底栏提示保持一致。

对应提交：`8faef9d0f0`，并由 `abc678f2a2` 补充合并后的 TUI 交互兼容处理。

## 构建与 Windows 发布能力

### 本地 release 构建

新增 `just` 入口：

```text
just build-i
just clean
```

`just build-i` 用于构建本地 release 版 Codex，并允许通过环境变量控制构建代理、并行任务数和 release LTO；Windows 与 Unix 使用对应的环境变量写法。`just clean` 只清理 release 产物，不影响 debug 产物。

### Windows x64 npm 发布

新增 `scripts/publish_personal_npm.ps1`，用于个人 Windows x64 发布：

- 构建主程序、code mode host、command runner 和 Windows sandbox setup 等配套二进制。
- 生成平台包和根 npm 包，自动配置平台依赖关系。
- 校验包名、版本、平台、CPU 和 `optionalDependencies` 映射。
- 默认执行 dry-run，只有显式传入 `-Publish` 才真正发布。
- 发布后查询版本，并通过安装后的 `--help` 和 `--version` 做最小运行校验。

对应提交：`8cd2337240`、`6197963ea9`。

## Plan 实施交接（开发中）

当前工作区新增了 Plan 确认后的专用交接路径，尚未提交到 Git：

- 继续当前上下文时，通过 `ContinueCurrentContext` 提交实施消息，不改变原有会话上下文。
- 选择清理上下文时，通过 `ClearContext` 创建新会话，仅携带明确的实施提示。
- ChatWidget 重建后仍保留本次交接的展示意图，避免上下文切换导致视觉状态丢失。
- 交接过程中显示居中的 `Plan Begins` 过渡效果，使用 Max 的橙金色展示，但不修改实际 reasoning effort。
- 终端禁用动画或启用 reduced motion 时不播放一次性动效。

该功能目前属于工作区开发状态，只有完成提交和后续验证后才能视为已落地。

## 工程支撑与质量改进

除产品功能外，个人提交还包含以下支撑性工作：

- 重构 Prompt 线程上下文，把参考内容限制为可控的 typed 投影，降低隐藏上下文和工具记录串入的风险。
- 修复 upstream 合并后的 TUI、core、MCP、keymap 和线程路由兼容问题。
- 清理失效的会话和渲染入口，减少无效路径继续参与状态管理。
- 为规则侧栏、Prompt 参数解析、鼠标定位、状态动效和 Plan 交接补充单元测试、集成测试或快照覆盖。

这些提交主要提升稳定性和可维护性，不单独计入产品功能数量。

## `$expand` 文档维护 Skill

项目新增 `.codex/skills/expand/SKILL.md`，用于显式调用 `$expand` 时补充更新本文；调用策略由 `.codex/skills/expand/agents/openai.yaml` 设为仅显式触发，避免普通 Git 任务误启动文档同步。

`$expand` 采用增量同步，不会每次重写整篇文档。它以当前 Git author、个人提交、分支状态、工作区 diff 和现有 `Expand.md` 为事实源：

- 保留已有人工内容、功能章节和有效 commit 证据，只补充新的事实。
- 将产品功能、工程支撑、merge/upstream、发布维护和未提交改动分开整理。
- 已提交能力标记为 `已落地`，工作区能力标记为 `开发中`，不把未提交代码描述成已发布功能。
- 不回滚、暂存或提交其他工作区改动；文档更新完成后检查 UTF-8、项目换行风格、尾随空格和 `git diff --check`。

本 skill 和本次文档维护改动当前尚未提交到 Git，属于开发中能力。

## Git 提交索引

| 日期 | 提交 | 内容归类 |
| --- | --- | --- |
| 2026-07-21 | `4ab57978ad` | 规则侧栏与快捷键改造，功能 |
| 2026-07-21 | `48a7618fc0` | 规则数据初始化，功能支撑 |
| 2026-07-21 | `0a2dd101b6` | 覆盖层输入与鼠标处理，功能 |
| 2026-08-13 | `1b31122314` | 输入标记间距与光标基线，功能 |
| 2026-08-13 | `24685f0969` | 首字符规范化与粘贴保留，功能 |
| 2026-08-14 | `28af02290a` | 隔离 Prompt 优化线程和主线程回填，功能 |
| 2026-08-14 | `149e263f59` | Prompt 模式交互提示，功能 |
| 2026-08-14 | `f128480824` | Side/Prompt 子线程禁用 MCP，安全支撑 |
| 2026-08-14 | `72090f0b85` | Prompt 优化参数和交互完善，功能 |
| 2026-08-14 | `8cd2337240` | 本地构建与清理命令，工程能力 |
| 2026-08-15 | `e32c0f6c84` | Prompt 优化协议与隔离 runtime，功能支撑 |
| 2026-08-16 | `70ecaae769` | Prompt 线程和 TUI 鼠标定位，功能 |
| 2026-08-17 | `8faef9d0f0` | Prompt/Side 底栏统一，功能 |
| 2026-08-17 | `edd9702168` | 工作区快照和测试收尾，工程支撑 |
| 2026-08-17 | `888ced5284` | 合并 upstream，不单独计为功能 |
| 2026-08-17 | `abc678f2a2` | 合并后的 TUI/core 兼容修复，工程支撑 |
| 2026-08-17 | `ed54de15a0` | 合并 upstream，不单独计为功能 |
| 2026-08-19 | `6197963ea9` | Windows x64 个人发布流程，工程能力 |
| 2026-08-20 | `446dcf37c1` | Prompt 线程上下文重构，功能支撑 |
| 2026-08-20 | `a0594836c4` | 清理失效会话与渲染入口，工程支撑 |

当前工作区的 Plan 实施交接和 `$expand` 文档维护改动尚无 commit id，按“开发中”记录。

## 结论

个人新增能力集中在三条主线：让规则可见、让提示词可优化、让 TUI 输入更自然；同时补齐了子线程隔离、Windows 发布和本地构建能力。整体改动没有把优化线程变成第二个执行线程，而是明确保持“Prompt 负责改写，主线程负责执行”的产品边界。
